# Pair turn

- **slice:** K (`fix/judge-reliability`)
- **driver:** Codex (keystore) then Cursor (GHA env + this protocol)
- **navigator next:** Codex — disagree-review the K PR; no product edits unless `FIX` requires a tiny patch
- **after ship K:** Cursor drives **L**, Codex navigates
- **keyboard:** released after the pair-protocol / GHA commit on this branch
