name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  contract:
    runs-on: ubuntu-latest
    env:
      SOTTO_JUDGE_KEYSTORE: isolated-file
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - name: Python deps
        run: python3 -m pip install pytest
      - name: Graph + Python contracts
        run: make graph && python3 -m pytest tests/contract -q --tb=short
      - name: Rust contracts (no GTK)
        run: cargo test --manifest-path src-tauri/Cargo.toml --no-default-features --lib --test contract
      - name: Demo
        run: cargo run --manifest-path src-tauri/Cargo.toml --no-default-features --quiet --bin sotto-demo
      - name: UI check
        run: npm ci && npm run check
