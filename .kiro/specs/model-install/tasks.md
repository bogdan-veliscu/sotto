# Model install — Tasks

- [ ] T11-checksum — `install_bytes` verifies SHA-256; mismatch leaves no file. CT-checksum, CT-no-silent-cloud
- [ ] T12-parakeet-local — overlay `ready` after install, `ENGINE_NOT_INSTALLED` when missing, delete roundtrip. CT-parakeet-local
