# Parakeet runtime — Tasks

- [x] T33-status — `parakeet_runtime_status` is `not-built` without a compiled decoder, `ready` when `parakeet-rs` is compiled in. CT-parakeet-runtime-status
- [x] T34-not-fixture — installed dummy blob never yields the CONSULT-001 transcript. CT-parakeet-not-fixture
- [x] T-decode — `parakeet-rs` TDT inference compiled into `desktop`. Dummy `.bin` is `ENGINE_MODEL_INVALID`. Real weights are a local TDT directory.
