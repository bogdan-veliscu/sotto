# Live capture — Tasks

- [ ] T7-wav-chunks — `record_sine` + `ChunkedRecorder` flush/stop. CT-capture-wav, CT-pause-resume, CT-crash-partial, CT-demo-still-offline
- [ ] T8-mic-error — `start_live(System)` → `CAPTURE_UNSUPPORTED` recoverable. Optional CPAL mic behind the same API. CT-mic-unsupported-is-recoverable
