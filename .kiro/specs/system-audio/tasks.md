# System audio — Tasks

- [x] T29-status — `system_tap_status` honest on Linux vs macOS. CT-system-tap-status
- [x] T30-tap — `start_live(System)` never uses CONSULT-001; unsupported stays recoverable. CT-system-not-fixture
